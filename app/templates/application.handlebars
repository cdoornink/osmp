<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container"> 
      <!-- Be sure to leave the brand out there if you want it shown -->
      <a class="brand" href="#">Recordinate</a>
      <ul class="nav">
        <li>{{#link-to 'projects'}} Browse {{/link-to}}</li>
        {{#if App.me.content.firstName}}
        <li>{{#link-to 'my-projects'}}My Projects {{/link-to}}</li>
        {{/if}}
      </ul>
      <ul class="nav  pull-right">
        {{#if App.me.content.firstName}}
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            {{App.me.content.firstName}} {{App.me.content.lastName}}
              <b class="caret" style="color:white"></b>
            </a>
            <ul class="dropdown-menu">
              <li>{{#link-to 'create'}}Create Project{{/link-to}}</li>
              <li>{{#link-to 'signout'}}Sign out{{/link-to}}</li>
            </ul>
          </li>
        {{else}}
          <li>{{#link-to 'signin'}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</div>
<div class="main container">{{outlet}}</div>