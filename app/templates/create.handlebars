<div class="upload-page">
  <form class="form-horizontal offset2 span6">
    <h2>New Project</h2>
    <legend>Basic Information</legend>
    <div class="control-group">
      <label class="control-label">Project Name</label>
      <div class="controls">{{view Ember.TextField valueBinding="name"}}</div>
    </div>
    <div class="control-group">
      <label class="control-label">Description</label>
      <div class="controls">{{view Ember.TextArea valueBinding="description"}}</div>
    </div>
    <div class="control-group">
      <label class="control-label">Genre</label>
      <div class="controls">{{view Ember.Select contentBinding="App.genres" valueBinding="genre"}}</div>
    </div>   
    <legend>Project Needs</legend>
    <div class="control-group">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need1"}}</div>
    </div>
    <div class="control-group">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details1"}}</div>
    </div>
    <div class="control-group need2">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need2"}}</div>
    </div>
    <div class="control-group need2">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details2"}}</div>
    </div>
    <div class="control-group need3">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need3"}}</div>
    </div>
    <div class="control-group need3">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details3"}}</div>
    </div>  
    <div class="control-group need4">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need4"}}</div>
    </div>
    <div class="control-group need4">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details4"}}</div>
    </div>  
    <div class="control-group need5">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need5"}}</div>
    </div>
    <div class="control-group need5">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details5"}}</div>
    </div>  
    <div class="control-group need6">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need6"}}</div>
    </div>
    <div class="control-group need6">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details6"}}</div>
    </div>  
    <div class="control-group need7">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need7"}}</div>
    </div>
    <div class="control-group need7">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details7"}}</div>
    </div>  
    <div class="control-group need8">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need8"}}</div>
    </div>
    <div class="control-group need8">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details8"}}</div>
    </div>  
    <div class="control-group need9">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need9"}}</div>
    </div>
    <div class="control-group need9">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details9"}}</div>
    </div>  
    <div class="control-group need10">
      <label class="control-label">Role</label>
      <div class="controls">{{view Ember.Select contentBinding="App.parts" valueBinding="need10"}}</div>
    </div>
    <div class="control-group need10">
      <label class="control-label">Details</label>
      <div class="controls">{{view Ember.TextArea valueBinding="details10"}}</div>
    </div>  
    <div class="control-group add-need-button">
      <div class="controls"><button class="btn btn-info" {{action "addNeed"}}>Add Another Need</button></div>
    </div>
    <div class="control-group">
      <div class="controls">
        <label class="checkbox">
          {{view Ember.Checkbox checkedBinding="needsMixing"}} I'd like other members to mix the project for me
        </label>
      </div>
    </div>
    <div class="control-group">
      <div class="controls">
        <label class="checkbox">
          {{view Ember.Checkbox checkedBinding="agreeToTerms"}} I agree to the {{#link-to 'terms'}}terms of use{{/link-to}}
        </label>
        <button type="submit" class="btn btn-primary"  {{bindAttr disabled="hasNotAgreed"}} {{action "submitProject"}}>Create Project</button>
      </div>
    </div>
  </form>
</div>
